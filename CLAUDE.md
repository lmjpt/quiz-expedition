# CLAUDE.md

**퀴즈 탐험대** 🧭 — 우리 아이들(2학년, 6살)이 PC 앞에 둘이 앉아서 하는 퀴즈
보드게임입니다. (처음 이름은 "말판 퀴즈"였는데 모험 지도 컨셉에 맞게 바꿨습니다.
화면 문구에서 "말판 퀴즈"를 발견하면 고쳐 주세요.)
이 문서는 이 저장소에서 작업할 때 따라야 할 규칙과 맥락을 담고 있습니다.

> 현재 상태: **첫 판이 돌아갑니다.** 시작 → 말판 레이스 → 퀴즈 → 도착 화면까지.
> 서버 없음, 로그인 없음, 기기(localStorage)에만 저장.

## 1. 무엇을 만드는가

주사위를 굴려 말을 옮기는 48칸(8×6) 레이스입니다. **차례마다 먼저 그 아이 수준의 문제
하나를 풀고, 맞히면 주사위를 굴립니다. 틀리면 그 차례는 굴리지 못하고 다음 사람.**
퀴즈 칸(❓)에 떨어지면 보너스 문제가 하나 더 나오고, 맞히면 2칸 더 갑니다.
먼저 도착하면 이깁니다.

한 판에 한 사람이 10~15문제를 풉니다. 처음에는 퀴즈 칸에 떨어질 때만 문제가
나왔는데 그러면 한 판에 4문제쯤이라 너무 적었고, 그 다음엔 "틀려도 굴리고 맞히면
주사위 +2" 였는데 "맞혀야 굴린다" 가 아이에게 더 명확해서 지금 규칙이 됐습니다.
"틀리면 못 굴린다" 는 뒤로 가는 벌은 아니므로 아래 원칙과 어긋나지 않습니다.

- **보드게임이 주인공, 퀴즈는 통행세.** 아이가 켜는 이유는 주사위와 말판이어야
  합니다. 학습 요소를 앞세우면 "숙제"가 되어 일주일 뒤 방치됩니다.
- **운이 실력을 덮어야 합니다.** 6살이 2학년을 이길 수 있어야 둘이 계속 합니다.
  주사위, 사다리, 미끄럼틀, 자리 바꾸기가 그 역할입니다.
- **틀린 벌은 없습니다.** 틀리면 그 자리, 맞히면 보너스. 뒤로 가는 것은 미끄럼틀
  칸(운)만입니다. 이 규칙은 `engine.ts` 맨 위에도 적혀 있습니다. 어기지 마세요.
- **한 판 15분 안팎.** 사람당 10~14문제. 처음엔 30칸이었는데 "너무 금방 끝난다"
  는 말에 48칸으로 키웠습니다. 더 길게/짧게 하려면 칸 수보다 주사위 눈이나
  보너스 칸 수를 먼저 만지세요.

### 설계 의도 (중요)

- **6살도 혼자 풉니다.** 모든 문제는 소리로 읽어 줍니다 (`speech.ts`, 브라우저
  내장 음성). 1~2단계 문제는 짧은 문장 + 그림(이모지) 보기입니다.
- **틀렸을 때 빨간색을 쓰지 않습니다.** "아쉬워요, 다음엔 맞힐 거예요" 와 정답,
  한 줄 설명. 화면이 혼내는 느낌이면 아이가 문제 앞에서 얼어요.
- **시간 제한 없음.** 시간 압박은 잘하는 아이만 좋아합니다.
- **누가 먼저 하는지는 판마다 정합니다.** 기본은 주사위로(둘이 한 번씩 굴려 높은 눈,
  같으면 다시 — `OrderPicker`), 아니면 번갈아(지난 판에 먼저 한 아이의 반대편). 매번
  첫째가 먼저 시작해서 둘째가 서운하다는 말에 만들었습니다. 말 색은 순서가 아니라
  아이(프로필 순서)에 고정입니다 — `PlayerState.color`.
- **말이 멈추면 1.5초 쉬고 문제 창을 엽니다** (`Play.tsx` 의 `LAND_PAUSE_MS`). 그 사이
  말판 위에 "새봄, 12번 칸 도착!" 안내가 뜹니다. 처음엔 0.3초였는데 어디에 섰는지
  볼 틈이 없다는 말을 들었습니다. 빨라 보인다고 이 틈을 줄이지 마세요.
- **PC 우선.** 마우스와 키보드(Space 굴리기, 1~4 보기 고르기, 숫자 입력)를 둘 다
  지원합니다. 폰 대응은 나중 일입니다.
- **가족 앱(`../test`)과 연결하지 않습니다.** 가족 앱은 "부모가 확인할 때만
  포인트가 붙는다" 원칙인데, 게임이 점수를 자동으로 넣으면 그 원칙이 깨집니다.
  연결하고 싶어지면 "게임 기록을 부모가 보고 칭찬을 준다" 방향으로만.

### 두 번째 놀이: 세계 여행 여권 (`src/games/passport/`)

첫 화면(`screens/Hub.tsx`)에서 말판 모험과 여권 놀이 중 고릅니다. 프로필·단계·문제
은행·소리·저장은 공유합니다. 새 놀이를 또 넣을 때도 `games/<이름>/` 에 두고 Hub 에
카드 하나를 더합니다.

- 만화풍 SVG 세계 지도(`WorldMap.tsx`, 그림 파일 없음)에 나라 30곳이 이모지 핀.
  유럽에 9곳이 몰려 있어서 "🔍 유럽 확대" 돋보기가 있습니다 (viewBox 를 유럽 영역으로
  바꾸고 핀 위치를 다시 계산. 확대 영역의 가로세로 비율은 전체 지도와 같아야 합니다).
  나라를 누르면 ✈️ 가 날아가고, **그 나라 문제 3개**를 풉니다. 나라마다 문제가 6개
  (쉬움 2, 보통 2 — 하나는 "어느 대륙?" 자동, 어려움 2 — 수도 + 하나) 있고, 낮은
  단계(1~2)는 쉬움+보통에서, 높은 단계는 보통+어려움에서 매번 3개를 섞어 뽑습니다.
  처음엔 상식 문제 하나를 섞었는데 "그 나라 문제 위주로" 라는 말에 뺐습니다.
- **3개 중 2개 맞히면 도장.** 못 받아도 벌은 없고 "다음에 다시 와요". 어느 쪽이든
  그 나라 이야기 한 줄을 읽어 줍니다.
- 도장은 아이마다 `storage.passport` 에 남아 판이 끝나도 쌓입니다. 이기고 지는 게
  없는 **수집 놀이**입니다. 둘이 하면 번갈아 한 나라씩 여행합니다.
- 나라 데이터와 나라별 문제는 `countries.ts` 한 파일. 나라를 더할 때는 지도 대륙
  모양 안에 핀이 놓이는지 보고, 손으로 쓰는 문제 4개(쉬움 2·보통 1·어려움 1)와
  이야기 두 줄을 같이 씁니다. 수도·대륙 문제는 자동으로 붙습니다.
- 연산·일반 상식 문제는 여기 안 나옵니다. 여행에는 그 나라 이야기만.
- `countries.ts` 가 24KB 로 사내망 업로드 한도(30KB) 에 가깝습니다. 나라를 더 넣을 때는
  먼저 대륙별 파일(`countries/europe.ts` …)로 쪼개세요.

## 2. 기술 스택

- React 19 + TypeScript (strict), Vite, Tailwind CSS v4
- 가족 앱(`../test`)과 같은 스택입니다. 배포 파이프라인(GitHub Pages)도 같습니다.
- **서버 없음.** Supabase 도, 로그인도 없습니다. 필요해지기 전에 붙이지 마세요.
- **Canvas·게임 엔진 없음.** 말판, 주사위, 말 이동은 DOM + CSS 로 충분합니다.
- 상태 관리 라이브러리 없음. `useReducer` 하나로 게임 상태를 돌립니다.

## 3. 구조

```
src/
  types.ts           Level, Subject, Question, Profile — 공용 타입과 표시 이름
  game/
    board.ts         말판 세 장(숲·바다·우주) 정의: 칸 배치, 색, 연결 이름, 격자 위치
    engine.ts        진행 규칙. 순수 함수 reduce(state, action). 화면·타이밍을 모름
    storage.ts       localStorage 전부 (프로필, 최근 문제, 틀린 문제, 소리 끔)
  questions/
    knowledge.ts     상식 문제 은행 합치기. 본문은 bank/level1~5.ts (단계별, 손으로 씀), 도우미 bank/q.ts
    arithmetic.ts    연산 문제 자동 생성, 단계별 규칙
    pick.ts          다음 문제 고르기 (연산:상식 반반, 최근 문제 피하기, 보기 섞기)
  speech.ts          문제 읽어 주기 (Web Speech API)
  sound.ts           효과음. 파일 없이 Web Audio 로 합성 (주사위·콩콩·딩동댕·사다리·팡파르)
  screens/           Start(누가·몇 단계·1인/2인) / Play(말판) / Finish(도착)
  components/        Board(모험 지도) / BoardPath(길·지나온 길·사다리 SVG) / Dice / QuizModal
                     / PlayerCard(판 도중 단계 바꾸기) / QuitDialog
```

- **말판은 격자가 아니라 지도입니다.** 칸은 구불구불한 길 위의 둥근 징검돌이고,
  특수 칸만 크게, 출발지와 목적지는 랜드마크로 세웁니다. 빈 자리에는 말판 테마의
  풍경 이모지를 흩어 놓습니다. 두 아이가 지나온 길은 각자 색으로 칠합니다.
  "흰 네모 상자 격자" 로 되돌아가지 마세요 — 그게 처음 모양이었고 조잡하다는 말을
  들었습니다. 글꼴은 화면 전체가 Google Fonts 의 **Jua** 하나(굵고 둥근 글꼴).
  손글씨 계열(Gaegu)을 시도했다가 획이 가늘어 6살이 읽기 어렵다는 말에 되돌렸습니다.
  가느다란 손글씨 글꼴은 다시 쓰지 마세요. Jua 는 굵기가 하나라 bold 가 안 먹으니
  강조는 크기로 합니다. 인터넷이 없으면 맑은 고딕으로 보입니다.

- **브라우저 기본 팝업(`confirm`, `alert`)을 쓰지 마세요.** Claude 앱 안 브라우저에서
  막히고, 아이 화면에 시스템 창이 뜨는 것도 어울리지 않습니다. 확인 창은 직접
  그립니다 (`QuitDialog` 참고).
- **단계는 판 도중에도 바꿀 수 있습니다.** 놀이 화면의 아이 카드를 누르면 됩니다.
  바꾼 값은 프로필에도 저장돼 다음 판과 시작 화면에 이어집니다. 나가서 바꾸고
  다시 들어오게 만들지 마세요.

- **규칙은 `engine.ts` 에만.** 화면(`Play.tsx`)은 타이밍, 소리, 키보드, 기록만
  맡습니다. "맞히면 몇 칸" 같은 걸 화면 코드에 쓰지 마세요.
- **말판은 세 장(숲·바다·우주)이고 판마다 돌려 씁니다.** `board.ts` 의 `BOARDS`.
  칸 배치와 색, 이름(사다리/파도타기/로켓)이 다르고 규칙은 같습니다. 무작위로
  섞지 마세요 — 손으로 그려야 균형이 지켜집니다. 마지막에 놀았던 말판은 기기에
  저장되고 다음 판은 그 다음 말판입니다. 시작 화면에서 직접 고를 수도 있습니다.
- **올라가는·내려가는 칸은 도착 칸(`to`)을 직접 적습니다.** 화면이 두 칸을 실제로
  잇는 그림을 그리므로, 올라가는 칸은 윗줄로만, 내려가는 칸은 아랫줄로만 놓으세요.
  줄 배치 표가 `board.ts` 주석에 있습니다. 큰 사다리(두 줄 위)는 한 장에 하나만 —
  운이 실력을 덮어야 하지만 판이 뒤집히기만 하면 재미가 없습니다.
- **단계(Level)는 1~5** (새싹·나무·숲·산·별). 나이가 아니라 "지금 풀 수 있는 것"
  기준입니다. 처음엔 그 아래 '씨앗'(그림 세기)이 있었는데 6살 둘째에게도 너무 쉬워서
  뺐고, 저장돼 있던 단계 번호는 `storage.ts` 의 `migrate` 가 하나씩 내렸습니다.
  "n 다음 수는?" 같은 문제도 같은 이유로 없습니다. 단계를 늘리거나 줄일 때는
  `types.ts`(Level, LEVELS, LEVEL_INFO), `storage.ts`(isLevel, migrate),
  `arithmetic.ts`(generateMath), `bank/levelN.ts` + `knowledge.ts` 를 같이 고치고
  저장 형식 `VERSION` 을 올립니다.
- **연산은 열에 셋** (`pick.ts` 의 `MATH_SHARE`). 반반이었는데 산수가 너무 자주 나온다는
  말을 들었습니다. 부모가 시작 화면이나 놀이 화면에서 고릅니다. 자동 조정은 아직 없습니다.
- **과목은 여덟 가지**: 수학(자동 생성), 과학, 역사 인물, 과학자, 세상 상식, 넌센스, 지리, 영어.
  영어는 1~2단계는 낱말→그림, 3단계부터 요일·인사말·짧은 문장, 5단계에 복수형·과거형.
  영어 낱말도 한국어 음성이 읽어 줍니다(발음은 어색하지만 6살에겐 충분).
  지리는 여행을 좋아하는 아이들용 — 대륙·나라·수도·명소·우리나라 지역. **국기
  이모지(🇰🇷)는 쓰지 마세요.** 윈도우 크롬에서는 글자 두 개(KR)로만 보입니다.
  넌센스는 아이들이 가장 좋아하는 과목입니다. 1~2단계는 수수께끼(생각해서 맞히는
  것), 3단계부터 말장난. **넌센스는 explain 을 꼭 넣으세요** — 답을 듣고 "아~" 하며
  웃는 게 핵심이라, 왜 그게 답인지 한 줄이 있어야 합니다.

## 4. 문제 은행 규칙 (`questions/bank/level1~5.ts`)

- **정답은 항상 첫 번째 보기.** 화면에 낼 때 섞습니다. 은행에서 섞어 쓰지 마세요.
- **한 문장, 짧게.** 1~2단계는 6살이 읽을 수 있는 길이. 4지선다, 그림 보기 환영.
- **역사는 '누가 무엇을 했나' 수준.** 연도, 사건 이름 암기는 넣지 마세요.
  2학년에게 역사는 인물 이야기입니다. 과학자도 같습니다.
- **explain 은 한 줄, "~예요" 로.** 틀렸을 때 읽어 주는 말입니다.
- **문장을 고치면 새 문제가 됩니다.** id 가 문장에서 나오기 때문입니다. 아이의
  '최근 나온 문제' 기록에서 빠지는 것뿐이니 부담 없이 고치세요.
- **한 단계에 최소 30개.** 그보다 적으면 한 판에 같은 문제가 겹칩니다.
- 연산은 손으로 쓰지 않습니다. 단계별 규칙은 `arithmetic.ts` 에서만 바꿉니다.
- **연산은 전부 숫자를 직접 넣게 합니다.** 보기 4개 중 고르기는 찍어도 넷에 하나가
  맞아서 연습이 안 됩니다.

## 5. 코딩 규칙

- **한국어 UI.** 화면 문구는 전부 한국어. 코드·주석은 파일 안에서 일관되게.
- `any` 금지. 모르면 `unknown` 으로 두고 좁히세요.
- 새 npm 패키지를 추가하기 전에 먼저 물어보세요.
- 컴포넌트가 200줄을 넘으면 쪼갤 때가 된 겁니다.

## 6. 명령어

```bash
npm install      # 의존성 설치
npm run dev      # 개발 서버 (http://localhost:5174 — 가족 앱 5173 과 겹치지 않게)
npm run build    # 타입 체크 + 프로덕션 빌드
npm run lint     # 린트
```

작업을 끝내기 전에 `npm run build` 와 `npm run lint` 가 모두 통과하는지 확인하세요.

> **사내망 참고**: 가족 앱과 같은 이유로 `.npmrc` 에 `strict-ssl=false` 가 있고
> git 에는 올리지 않습니다. 회사 프록시는 `git push` 도 403 으로 막습니다. 그래서
> 사내망에서는 GitHub API(`gh api`)로 커밋을 조립해 올리는데(`bash scripts/api-push.sh`,
> Git Bash 에서 실행 — PowerShell 은 한글 커밋 메시지가 깨집니다), **파일 하나가 30KB 를
> 넘으면 그것도 막힙니다.** 문제 은행을 `questions/bank/level1~5.ts` 로 나눠 둔 이유가
> 이것입니다. 한 파일이 25KB 를 넘어가면 쪼개세요. `package-lock.json` 은 올리지
> 못해서 `.github/workflows/lockfile.yml` 이 GitHub 쪽에서 만듭니다. 집에서는 그냥
> `git push` 하면 됩니다.

## 배포

GitHub 저장소 `lmjpt/quiz-expedition`, `main` 에 푸시하면 GitHub Pages 로 자동 배포.
주소: https://lmjpt.github.io/quiz-expedition/ (Supabase 같은 비밀 값은 없어서 Secrets 설정이 필요 없습니다.)

## 7. 다음에 할 만한 것 (아직 안 한 것)

우선순위 순입니다. 위에 있는 것부터, 아이가 실제로 하는 걸 본 뒤에.

1. **아이가 해 보고 나서 고치기.** 문제 난이도, 한 판 길이, 소리 속도와 크기.
2. **자동 난이도.** 연속 정답이면 한 단계 위 문제를 섞어 넣기.
3. **부모용 기록 화면.** `storage.ts` 의 `missed` 를 보여 주기만 하면 됩니다.
4. **문제 은행 다듬기.** 지금 단계당 56~77개. 개수보다 아이가 실제로 틀린 문제를
   보고 "이건 너무 어렵다/쉽다" 를 단계 사이에서 옮기는 게 먼저입니다. 과목 균형은
   과학이 가장 많고 세상 상식이 가장 적습니다. 4단계 세상 상식이 특히 적습니다.

넣지 않을 것: 서버, 로그인, 랭킹, 결제, 시간 제한, 틀렸을 때 페널티.
